# Example Model A

这是 **ML Pages** 的示例模型详情页内容，用于验证：

- Markdown 渲染（标题、列表、代码块）
- 路由参数与内容加载（`/models/:id`）

## 概览

- **定位：** 示例内容
- **任务：** Text Generation
- **备注：** 这里的字段仅用于展示页面排版

## 使用示例

```python
# 示例内容：伪代码，仅用于展示代码块样式
def generate(text: str) -> str:
	return "[example output] " + text

print(generate("hello"))
```

## 说明

此页面不包含真实模型信息。
